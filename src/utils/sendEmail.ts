export function sendVerficationEmail(
  to: string,
  subject: string,
  text: string,
) {
  // Placeholder function for sending email
  console.log(`Sending email to: ${to}`);
  console.log(`Subject: ${subject}`);
  console.log(`Text: ${text}`);
}